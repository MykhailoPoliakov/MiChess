use eframe::egui;

pub struct Render {
    state: &State,
}


impl Render {
    pub fn new(state: &State) -> Self {
        Render {
            state: &State,

        }
    }

    pub fn board(&mut self) -> () {

    }
}